import streamlit as st

def show_theoretical_framework():
    st.title("📊 공공데이터 품질관리 - 이론 프레임워크")
    
    st.header("7대 품질 지표 및 진단 방법 이론")
    
    with st.expander("1. 준비성 (Availability) 지표", expanded=True):
        st.subheader("가. 관리요소 특성")
        st.markdown("""
        - **특성 정의**: 데이터 품질관리 기본 구성요소를 정의
        - **진단 기준**: 
          - 데이터 관리를 위한 정책, 규정, 지침, 절차 등 존재 여부
          - 기관의 데이터 관리 목적과 규모에 맞는 조직, 인력, 역할이 정의 여부
          - 데이터 품질 관리를 위한 표준 문서들이 정의 여부
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("나. 내용충실 특성")
        st.markdown("""
        - **특성 정의**: 정의한 구성 요소의 내용이 빠짐없이 기술되고 변화관리
        - **진단 기준**: 
          - 데이터 관리 규정 준수 여부
          - 조직(역할)이 적정하게 부여 및 배정 여부
        - **진단 방법**: 체크리스트
        """)

    with st.expander("2. 완전성 (Completeness) 지표", expanded=True):
        st.subheader("가. 논리모델 특성")
        st.markdown("""
        - **특성 정의**: 데이터 모델의 완전성은 데이터 품질관리의 가장 기본 사항
        - **진단 기준**: 
          - 논리적으로 적절히 분할되어 있는지 여부
          - 반드시 값이 채워져야 하는 속성들이 정의 여부
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단
        """)
        
        st.subheader("나. 식별자 특성")
        st.markdown("""
        - **특성 정의**: 모든 엔터티는 각 레코드를 유일하게 구분할 수 있는 식별자 존재
        - **진단 기준**: 
          - 엔터티 내의 레코드를 유일하게 구분할 수 있는 식별자 정의 여부
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단
        """)
        
        st.subheader("다. 물리구조 특성")
        st.markdown("""
        - **특성 정의**: 논리 데이터 모델에서 정의한 데이터 규칙을 반영하여 물리구조 구현
        - **진단 기준**: 
          - 논리 데이터 모델과 물리 데이터 모델의 정합성 여부
          - 데이터 검증 규칙 정의 여부
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단
        """)
        
        st.subheader("라. 속성의미 특성")
        st.markdown("""
        - **특성 정의**: DB에 저장된 속성의 데이터 값은 각기 독립된 의미를 가져야 함
        - **진단 기준**: 
          - 엔터티의 속성은 고유한 특성이나 성질을 갖고 더 이상 분해할 수 없도록 정의
        - **진단 방법**: 체크리스트, 프로파일링
        """)

    with st.expander("3. 일관성 (Consistency) 지표", expanded=True):
        st.subheader("가. 속성 특성")
        st.markdown("""
        - **특성 정의**: 같은 이름을 갖는 속성은 같은 의미로 사용되어야 함
        - **진단 기준**: 
          - 서로 다른 엔터티들 간에 속성이 같은 의미를 갖는다면, 같은 이름으로 정의
        - **진단 방법**: 체크리스트, 프로파일링
        """)
        
        st.subheader("나. 표준 특성")
        st.markdown("""
        - **특성 정의**: 기관내 표준 데이터 항목을 정의하고 범정부 표준항목 준수
        - **진단 기준**: 
          - 데이터 표준 적용 준수율을 주기적으로 점검
          - 범정부 공통표준용어 행정표준코드 준수 여부 확인
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단
        """)
        
        st.subheader("다. 중복값 특성")
        st.markdown("""
        - **특성 정의**: 중복 데이터는 하나의 DB내에서 원값이 존재하고 이를 복사하여 사용하는 값들
        - **진단 기준**: 
          - 동일 DB 내에서 두 개 이상 테이블에 존재하는 중복 데이터의 중복된 값과 원값의 구분
        - **진단 방법**: 체크리스트, 업무규칙 진단
        """)
        
        st.subheader("라. 연계값 특성")
        st.markdown("""
        - **특성 정의**: 기관 간 연계에 의해 DB간 이동이 일어나는 데이터
        - **진단 기준**: 
          - 시스템 간 이동되는 데이터를 알고 연동 주기와 방법이 문서화
        - **진단 방법**: 체크리스트, 업무규칙 진단
        """)

    with st.expander("4. 정확성 (Accuracy) 지표", expanded=True):
        st.subheader("가. 입력값 특성")
        st.markdown("""
        - **특성 정의**: 데이터 입력시 오류를 사전 검증(Edit)하고 수정(Imputation)하는 방법과 절차 정의
        - **진단 기준**: 
          - 데이터 입력 시 필수 데이터 누락을 방지하기 위한 방법 정의 여부
          - 데이터 입력 시 데이터 값이 유효성 지표에 맞는지 검증 여부
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단, 비정형 실측
        """)
        
        st.subheader("나. 업무규칙 특성")
        st.markdown("""
        - **특성 정의**: 법․규정에 의해 업무를 수행함에 있어 관리하는 데이터의 생성 규칙
        - **진단 기준**: 
          - 업무규칙 정의서 존재 여부
          - 업무규칙대로 데이터가 저장되었는지 검증 여부
        - **진단 방법**: 체크리스트, 업무규칙 진단
        """)
        
        st.subheader("다. 범위·형식 특성")
        st.markdown("""
        - **특성 정의**: DB에 저장된 데이터 값은 유효한 범위와 형식을 가져야 함
        - **진단 기준**: 
          - DB에 저장되는 값은 실제 업무 값과 같은 범위를 가지는지 여부
        - **진단 방법**: 체크리스트, 프로파일링
        """)
        
        st.subheader("라. 참조관계 특성")
        st.markdown("""
        - **특성 정의**: 참조관계 데이터 간의 값은 참조되는 값의 범위 내에 존재
        - **진단 기준**: 
          - 논리적으로 관계가 있는 테이블들의 속성이 참조 무결성을 준수 여부
        - **진단 방법**: 체크리스트, 프로파일링, 업무규칙 진단
        """)
        
        st.subheader("마. 계산식 특성")
        st.markdown("""
        - **특성 정의**: 계산 또는 집계된 값의 원값의 일부 혹은 전부는 DB에 이미 저장된 값
        - **진단 기준**: 
          - 원천데이터를 가공한 데이터의 경우 가공식을 이용해 재가공할 수 있는지 여부
        - **진단 방법**: 체크리스트, 업무규칙 진단
        """)

    with st.expander("5. 보안성 (Security) 지표", expanded=True):
        st.subheader("가. 오너십 특성")
        st.markdown("""
        - **특성 정의**: 데이터와 표준 데이터 품질관리를 위해 정보생명주기(생성, 갱신, 삭제)에 따른 담당자 배정
        - **진단 기준**: 
          - 주요 업무 데이터에 대해 현업 담당자(데이터 오너십)가 배정 여부
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("나. 접근제한 특성")
        st.markdown("""
        - **특성 정의**: 중요 데이터에 대한 보기 접근 권한이 설정
        - **진단 기준**: 
          - 보안이 특별히 더 강조되는 데이터의 접근 제어 위해 사용자 계정 및 암호 관리 정책
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("다. DB보호 특성")
        st.markdown("""
        - **특성 정의**: 데이터를 저장하고 관리하는 DBMS는 충분히 안전하게 보호
        - **진단 기준**: 
          - 공공데이터를 관리하는 DBMS는 통합 데이터 센터에서 관리 여부
        - **진단 방법**: 체크리스트
        """)

    with st.expander("6. 적시성 (Timeliness) 지표", expanded=True):
        st.subheader("가. 응답시간 특성")
        st.markdown("""
        - **특성 정의**: 정보는 사용자가 만족하는 응답 시간 내에 전달
        - **진단 기준**: 
          - 화면이나 프로그램 단위에서 기대 응답시간과 제공 응답시간 정의 여부
        - **진단 방법**: 체크리스트, 비정형 실측
        """)
        
        st.subheader("나. 데이터 제공 특성")
        st.markdown("""
        - **특성 정의**: 사용자에 의한 새로운 정보 요구가 있을 때, 서비스하기 위한 시간은 최소화
        - **진단 기준**: 
          - 요구사항에 대해 서비스하기까지 정의된 규칙과 절차 존재 여부
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("다. 최신값 특성")
        st.markdown("""
        - **특성 정의**: 실제 업무에서 변경된 값을 DB에 반영하기 위한 규칙과 절차 정의
        - **진단 기준**: 
          - 주요 데이터에 대해 변경 주기 관리 여부
        - **진단 방법**: 체크리스트
        """)

    with st.expander("7. 유용성 (Usability) 지표", expanded=True):
        st.subheader("가. 충분 특성")
        st.markdown("""
        - **특성 정의**: 사용자가 만족할 수 있는 충분한 데이터량을 수집 및 제공
        - **진단 기준**: 
          - 사용자 만족을 위한 충분한 양의 정보를 적시에 수집 여부
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("나. 접근 특성")
        st.markdown("""
        - **특성 정의**: 데이터는 사용자의 다양한 요구에 유연하게 대응할 수 있는 형식과 구조
        - **진단 기준**: 
          - 사용자가 접근활용하기 쉬운 정보 단위로 제공 여부
        - **진단 방법**: 체크리스트
        """)
        
        st.subheader("다. 활용 특성")
        st.markdown("""
        - **특성 정의**: 정보의 가치 측정을 위해 제공하는 정보의 사용자 만족도와 활용 현황 측정
        - **진단 기준**: 
          - 데이터가 어떤 경로로 얼마나 자주 접근되고 있는지 조사 여부
        - **진단 방법**: 체크리스트
        """)

    st.header("4대 진단 방법 이론")
    
    st.markdown("""
    ### 1. 프로파일링 (Profiling)
    - 데이터 값 또는 구조에 기초하여 데이터베이스를 구성하는 논리적, 물리적 데이터 구조 및 컬럼 등의 표준 준수 및 실제 저장된 데이터 값의 정확성, 유효성 등을 진단 도구를 사용하여 품질을 측정하는 방법
    - **값 진단**: 데이터 값의 유효성, 정확성 등 분석
    - **구조 진단**: 논리적 데이터 구조의 오류로 인한 일관성, 정합성 등을 확보하지 못하는 결함 분석
    
    ### 2. 업무규칙 진단 (Business Rule Diagnosis)
    - 법, 규정에 정의된 업무기준(산출식)에 근거하여 데이터가 관리되고 있는지 진단
    - 업무규칙(BR; Business Rule) 준수 여부를 측정하는 스크립트(SQL 등)를 실행하여 오류 값을 추출
    
    ### 3. 체크리스트 (Checklist)
    - 전반적인 데이터 품질관리 수준과 지표별 데이터 품질 수준을 설문 또는 인터뷰를 통해 진단
    
    ### 4. 비정형 실측 (Unstructured Data Measurement)
    - 문서, 이미지, 동영상 등 정형화되어 있지 않은 정보를 사람이 직접 확인(실측)하여 오류 여부를 진단
    - 별도의 도구 없이 정보를 조회하거나 해당 문서를 수기로 확인하는 방식으로 수행
    """)
    
    st.markdown("---")
    st.markdown("이 이론 프레임워크는 공공데이터 품질관리 매뉴얼 기준으로 작성되었습니다.")